# conf-extra

Additional components designed to be used with `conf`. For example, value parsers for `Duration`
and other common types that you might want in your config, but don't implement `FromStr`.

Many of the value parsers are also compatible with `clap`.

Most dependencies are optional, so you can turn on only what you need.
